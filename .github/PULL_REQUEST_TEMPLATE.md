## What changed and why

<!-- One or two sentences. Link the issue this resolves, if any. -->

## Checklist

- [ ] `mkdocs build --strict` passes locally
- [ ] `python3 scripts/lint_manual.py` passes (run after `mkdocs build`)
- [ ] Any factual change carries a `Source:` line pointing at what backs it
- [ ] Any factual change adds a row to the corrections log in `docs/manual/00-index.md`, per its "How to add a row" note
- [ ] Chapter format follows `docs/manual/CONVENTIONS.md` (header block, `### Step NN.M`, Parts / Do / Check, `⚠ Rev D+ / LDO:` callouts as needed)
